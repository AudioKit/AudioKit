//
//  AKMetronome.swift
//  AudioKit
//
//  Autogenerated by scripts by Aurelius Prochazka on 9/14/15.
//  Copyright (c) 2015 Aurelius Prochazka. All rights reserved.
//

import Foundation

/** Generator of a series of 1-sample ticks at a regular rate.

Typically, this is used alongside trigger-driven modules.
*/
@objc class AKMetronome : AKParameter {

    // MARK: - Properties

    private var metro = UnsafeMutablePointer<sp_metro>.alloc(1)

    /** The frequency to repeat. [Default Value: 1.0] */
    private var frequency: Float = 0



    // MARK: - Initializers

    /** Instantiates the metronome with default values
    */
    override init()
    {
        super.init()
        setup()
        dependencies = []
        bindAll()
    }

    /** Instantiates metronome with constants

    - parameter frequency: The frequency to repeat. [Default Value: 1.0]
    */
    init (frequency freqInput: Float) {
        super.init()
        setup(freqInput)
        bindAll()
    }

    // MARK: - Internals

    /** Bind every property to the internal metronome */
    internal func bindAll() {
    }

    /** Internal set up function */
    internal func setup(frequency: Float = 1.0)
 {
        sp_metro_create(&metro)
        sp_metro_init(AKManager.sharedManager.data, metro, frequency)
    }

    /** Computation of the next value */
    override func compute() {
        sp_metro_compute(AKManager.sharedManager.data, metro, nil, &leftOutput);
        rightOutput = leftOutput
    }

    /** Release of memory */
    override func teardown() {
        sp_metro_destroy(&metro)
    }
}
