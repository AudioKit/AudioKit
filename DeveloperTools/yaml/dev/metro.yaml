installation-directory: "Signal\ Generators/Musical\ Controls/"

sp-module: metro

node: AKMetronome

summary: Generator of a series of 1-sample ticks at a regular rate.

four-letter-code:

one-word-description: metronome

description:
  Typically, this is used alongside trigger-driven modules.

parameters:
- freq: {
  ak-variable: frequency,
  comment: "The frequency to repeat.",
  default: 2.0
}

