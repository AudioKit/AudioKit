installation-directory: "Effects/Volume\ and\ Spatialization/"

sp-module: panst

node: AKPanner

summary: Stereo Panner

four-letter-code:

one-word-description: panner

description: Stereo Panner

inputCount: 2
outputCount: 2

inputs:
- source: {
  ak-variable: input,
  comment: "Input audio signal."
}

parameters:

- pan: {
  ak-variable: pan,
  comment: "Panning. From -1 (hard left) to 1 (hard right)",
  default: 0.0
}
