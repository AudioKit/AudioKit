installation-directory: "Signal\ Generators/Random\ Generators/"

sp-module: random

node: AKRandomNumbers

summary: Random Number Generator

four-letter-code:

one-word-description: numbers

description:
  Generate random values within a range.

parameters:
- min: {
  ak-variable: lowerBound,
  comment: "Minimum range limit.",
  default: -0.2
}
- max: {
  ak-variable: upperBound,
  comment: "Maximum range limit.",
  default: 0.2
}

