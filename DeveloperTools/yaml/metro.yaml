installation_directory: "../AudioKit/Operations/Signal\ Generators/Musical\ Controls/"

sp_name: metro

operation: AKMetronome

summary: Generator of a series of 1-sample ticks at a regular rate.

shortDescription: metronome

description:
  Typically, this is used alongside trigger-driven modules.

parameters:
- freq: {
  audioKitName: frequency,
  comment: "The frequency to repeat.",
  default: 2.0
}

